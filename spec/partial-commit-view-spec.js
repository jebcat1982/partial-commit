'use babel'

/* global jasmine, define, it, runs, describe, expect, waitsForPromise, beforeEach */

describe('PartialCommitView', function() {
  return it('has one valid test', function() {
    return expect('easy').toBe('easy')
  })
})
